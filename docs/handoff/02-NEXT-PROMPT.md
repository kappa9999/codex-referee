# Next Prompt

Use this in a new Codex session:

"Continue in C:\\Users\\Administrator.DESKTOP-Q3M31LP\\codex-referee-project. Read docs/handoff/00-START-HERE.md and docs/handoff/01-CURRENT-STATE.md first. Enforce the no-shortcut endgame optimization mandate. Then implement the next milestone: persistence + decision loop. Build `@referee/store` run index and feedback storage, wire `referee inspect` to load run artifacts, implement `referee accept` and `referee feedback` commands to record outcomes, and add first store unit tests + migration-safe schema validation checks. Run `pnpm format && pnpm lint && pnpm typecheck && pnpm test && pnpm build` and update docs/planning/BACKLOG.md, docs/decisions/DECISION-LOG.md, docs/handoff/01-CURRENT-STATE.md, docs/handoff/02-NEXT-PROMPT.md, and logs/SESSION-LOG.md before ending."
